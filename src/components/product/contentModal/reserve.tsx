import Working from "@/components/global/working";
import styles from "@/styles/product/review.module.css";

const Reserve = () => {
  return (
    <div className={styles.commentsContainer}>
      <Working />
    </div>
  );
};

export default Reserve;
